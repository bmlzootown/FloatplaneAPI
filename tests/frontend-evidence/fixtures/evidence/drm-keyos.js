/* Vendor DRM noise — must be rejected, not structured_operation */
const fairplayCert="https://fairplay.twitch.keyos.com/api/v4/getCertificate";
fetch(fairplayCert);
/* Also a Floatplane path string without request construction */
const hint="/api/v3/somewhere/maybe";
